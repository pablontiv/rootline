{
  "permissions": {},
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "CODE=$(git log --oneline -10 --diff-filter=M -- '*.go' 'go.mod' | wc -l) && DOCS=$(git log --oneline -5 -- 'CLAUDE.md' 'README.md' 'docs/*.md' | wc -l) && if [ \"$CODE\" -gt 5 ] && [ \"$DOCS\" -eq 0 ]; then echo '{\"additionalContext\": \"Docs may be stale. Run /update-docs to sync.\"}'; fi"
          }
        ]
      }
    ],
    "PostToolUse": []
  }
}
